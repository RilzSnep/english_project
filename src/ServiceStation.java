// ServiceStation class for servicing transports
public class ServiceStation {

    // Method to check and service transport
    public void check(Transport transport) {
        transport.check(); // Calling check method
    }
}
